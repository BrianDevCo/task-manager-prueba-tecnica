═══════════════════════════════════════════════════════════════
  COMANDOS PARA GIT BASH (SI GIT NO ESTÁ EN PATH)
═══════════════════════════════════════════════════════════════

Si Git no está en el PATH, usa Git Bash (siempre funciona):

1. Busca "Git Bash" en el menú de inicio de Windows
2. Click derecho → "Ejecutar como administrador" (opcional)
3. Navega a la carpeta del proyecto:

   cd /c/Entrevista/Proyecto/TaskManager

4. Ejecuta estos comandos:

   git init
   git add .
   git config --global user.name "Tu Nombre"
   git config --global user.email "tu@email.com"
   git commit -m "Initial commit: Task Manager Fullstack .NET - Prueba Tecnica"
   git branch -M main

5. Crea repositorio en GitHub (ve a github.com)

6. Conecta y sube (reemplaza TU_USUARIO):

   git remote add origin https://github.com/TU_USUARIO/task-manager-prueba-tecnica.git
   git push -u origin main

═══════════════════════════════════════════════════════════════

¡Listo! 🚀


